<UserControl x:Class="XnViewPlaylistBuilder.Controls.RgbaColorEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Orientation="Horizontal">
        <TextBlock x:Name="LabelTextBlock"
                   Style="{StaticResource Text.Label}"
                   Width="96"
                   VerticalAlignment="Center"/>
        <Button x:Name="ColorButton"
                Width="36"
                Height="28"
                Padding="0"
                Margin="0,0,8,0"
                ToolTip="Click to choose a color"
                Click="ColorButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="SwatchBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource Brush.Border}"
                            BorderThickness="1"
                            CornerRadius="4"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SwatchBorder" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="SwatchBorder" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <TextBlock x:Name="SummaryTextBlock"
                   VerticalAlignment="Center"
                   Foreground="{StaticResource Brush.TextSecondary}"
                   FontFamily="{StaticResource Font.Mono}"
                   FontSize="11"/>
    </StackPanel>
</UserControl>
